package profiles

// SaveNavigationState implements the Page interface (empty for profiles page)
func (pp *ProfilesPage) SaveNavigationState() {
	// Profiles page doesn't need state management
}

// RestoreNavigationState implements the Page interface (empty for profiles page)
func (pp *ProfilesPage) RestoreNavigationState() {
	// Profiles page doesn't need state management
}

// ClearNavigationState implements the Page interface (empty for profiles page)
func (pp *ProfilesPage) ClearNavigationState() {
	// Profiles page doesn't need state management
}
