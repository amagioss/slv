package vault_new

// SaveNavigationState implements the Page interface (empty for vault new page)
func (vnp *VaultNewPage) SaveNavigationState() {
	// Vault new page doesn't need state management
}

// RestoreNavigationState implements the Page interface (empty for vault new page)
func (vnp *VaultNewPage) RestoreNavigationState() {
	// Vault new page doesn't need state management
}

// ClearNavigationState implements the Page interface (empty for vault new page)
func (vnp *VaultNewPage) ClearNavigationState() {
	// Vault new page doesn't need state management
}
