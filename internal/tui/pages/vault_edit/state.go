package vault_edit

// SaveNavigationState implements the Page interface (empty for vault edit page)
func (vep *VaultEditPage) SaveNavigationState() {
	// Vault edit page doesn't need state management
}

// RestoreNavigationState implements the Page interface (empty for vault edit page)
func (vep *VaultEditPage) RestoreNavigationState() {
	// Vault edit page doesn't need state management
}

// ClearNavigationState implements the Page interface (empty for vault edit page)
func (vep *VaultEditPage) ClearNavigationState() {
	// Vault edit page doesn't need state management
}
